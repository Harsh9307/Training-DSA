#include<bits/stdc++.h>
using namespace std;

int main(){
    string s;
    cin >> s;
    int noOfOperation;
    cin >> noOfOperation;

    vector<int> a ;
    for(int i=0;i<noOfOperation;i++){
        cin>>arr[i];
    }
    for(int i=0;i<noOfOperation;i++){
        int startIndex = a[i];
        int endIndex = s.size()-a[i]+1;
    }
}